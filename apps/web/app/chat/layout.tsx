export const metadata = {
  title: 'Night Nest - Chat',
  description: 'AI Character Chat',
}

export default function ChatLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
