export const metadata = {
  title: 'Night Nest - Simulator',
  description: 'AI Character Simulator',
}

export default function SimulatorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
